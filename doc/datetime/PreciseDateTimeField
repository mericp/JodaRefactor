Precise misc.datetime misc.field, composed of two precise misc.duration fields.
 * <p>
 * This DateTimeField is useful for defining DateTimeFields that are composed
 * of precise durations, like time of day fields. If either misc.duration misc.field is
 * imprecise, then an {@link ImpreciseDateTimeField} may be used instead.
 * <p>
 * PreciseDateTimeField is thread-safe and immutable.

 public PreciseDateTimeField(DateTimeFieldType type, DurationField unit, DurationField range):
 Constructor.
      *
      * @param type  the misc.field type this misc.field uses
      * @param unit  precise unit misc.duration, like "seconds()".
      * @param range precise range misc.duration, preferably a multiple of the unit,
      * like "minutes()".
      * @throws IllegalArgumentException if either misc.duration misc.field is imprecise
      * @throws IllegalArgumentException if unit milliseconds is less than one
      * or effective value range is less than two.

public int get(long misc.instant):
Get the amount of fractional units from the specified time misc.instant.
     *
     * @param misc.instant  the milliseconds from 1970-01-01T00:00:00Z to query
     * @return the amount of fractional units extracted from the input.

public long addWrapField(long misc.instant, int amount):
Add to the component of the specified time misc.instant, wrapping around
     * within that component if necessary.
     *
     * @param misc.instant  the milliseconds from 1970-01-01T00:00:00Z to add to
     * @param amount  the amount of units to add (can be negative).
     * @return the updated time misc.instant.

public long set(long misc.instant, int value):
Set the specified amount of units to the specified time misc.instant.
     *
     * @param misc.instant  the milliseconds from 1970-01-01T00:00:00Z to set in
     * @param value  value of units to set.
     * @return the updated time misc.instant.
     * @throws IllegalArgumentException if value is too large or too small.

public DurationField getRangeDurationField():
