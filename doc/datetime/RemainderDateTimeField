Counterpart remainder misc.datetime misc.field to {@link DividedDateTimeField}. The
 * misc.field's unit misc.duration is unchanged, but the range misc.duration is scaled
 * accordingly.
 * <p>
 * RemainderDateTimeField is thread-safe and immutable.
 *
 * @see DividedDateTimeField

 public RemainderDateTimeField(DateTimeField misc.field, DateTimeFieldType type, int divisor):
 Constructor.
      *
      * @param misc.field  the misc.field to wrap, like "year()".
      * @param type  the misc.field type this misc.field actually uses
      * @param divisor  divisor, such as 100 years in a century
      * @throws IllegalArgumentException if divisor is less than two